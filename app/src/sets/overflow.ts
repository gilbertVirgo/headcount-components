export interface props {
    "overflow-hidden"?: boolean,
    "overflow-scroll"?: boolean,
    "overflow-auto"?: boolean
}

export const sets = {
    "overflow-hidden": { overflow: "hidden" },
    "overflow-scroll": { overflow: "scroll" },
    "overflow-auto": { overflow: "auto" }
}